# Example account mapping (sanitized, no real account identifiers).
version: 1
accounts:
  - alias: primary_taxable
    broker: broker_template
    currency: KRW
    account_type: taxable
  - alias: retirement
    broker: broker_template
    currency: KRW
    account_type: pension
